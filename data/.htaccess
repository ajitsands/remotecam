# Deny ALL direct web access to the data directory.
# This protects session files, JSON state, snapshots and security logs.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>